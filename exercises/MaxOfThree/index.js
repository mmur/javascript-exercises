/**
 * MaxOfThree
 *
 * Given three numbers, write a function which returns the max of the three.
 *
 * Examples:
 * maxOfThree(4, 8, -2) === 8
 * maxOfThree(14, 82, 1) === 82
 */

function maxOfThree(x, y, z) {
}

module.exports = maxOfThree;
